<UserControl x:Class="Cafe_Managment.View.Order"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Cafe_Managment.View"
             xmlns:vm="clr-namespace:Cafe_Managment.ViewModel"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:OrderVM/>
    </UserControl.DataContext>
    <Grid
        >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <ScrollViewer VerticalScrollBarVisibility="Hidden"
                      Grid.Column="0">
            <ListBox ItemsSource="{Binding CategoryList, Mode=OneWay}"
                     SelectedIndex="{Binding SelectedCategory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     HorizontalAlignment="Center">
                <ListBox.Template>
                    <ControlTemplate>
                        <WrapPanel IsItemsHost="True"
                                   ItemWidth="100"
                                   />
                    </ControlTemplate>
                </ListBox.Template>
            </ListBox>
        </ScrollViewer>
        <ScrollViewer VerticalScrollBarVisibility="Hidden"
              Grid.Column="1">
            <ListBox
             
             HorizontalAlignment="Center">
                <ListBox.Template>
                    <ControlTemplate>
                        <WrapPanel IsItemsHost="True"
                           ItemWidth="150"
                           />
                    </ControlTemplate>
                </ListBox.Template>
            </ListBox>
        </ScrollViewer>
    </Grid>
</UserControl>
